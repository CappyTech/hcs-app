<div class="max-w-md mx-auto py-10 px-4">
  <div class="bg-white dark:bg-gray-900 shadow-lg rounded-2xl overflow-hidden">
    <div class="h-1.5 bg-gradient-to-r from-green-500 to-emerald-400"></div>

    <div class="p-8">
      <h1 class="text-2xl font-bold text-center text-gray-900 dark:text-gray-100 mb-2">Authenticator Verification</h1>
      <p class="text-center text-sm text-gray-500 dark:text-gray-400 mb-6">
        Choose a new password first, then open your authenticator app and enter the code that is
        showing at that moment.
      </p>

      <form action="/user/verify-totp-reset" method="POST" class="space-y-6">
        <%- include('../partials/csrfHidden.ejs') %>

        <!-- New password -->
        <div>
          <label for="password" class="block mb-1 text-sm font-medium text-gray-700 dark:text-gray-300">
            New password:
          </label>
          <input
            type="password"
            id="password"
            name="password"
            required
            <% if (!(locals.draft && draft.password)) { %>autofocus<% } %>
            minlength="6"
            value="<%= locals.draft && draft.password || '' %>"
            autocomplete="new-password"
            class="w-full px-4 py-2.5 border border-gray-200 dark:border-gray-700 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-500 bg-gray-50 dark:bg-gray-800 text-gray-900 dark:text-gray-100"
          >
        </div>

        <!-- Confirm password -->
        <div>
          <label for="confirmPassword" class="block mb-1 text-sm font-medium text-gray-700 dark:text-gray-300">
            Confirm new password:
          </label>
          <input
            type="password"
            id="confirmPassword"
            name="confirmPassword"
            required
            minlength="6"
            value="<%= locals.draft && draft.confirmPassword || '' %>"
            autocomplete="new-password"
            class="w-full px-4 py-2.5 border border-gray-200 dark:border-gray-700 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-500 bg-gray-50 dark:bg-gray-800 text-gray-900 dark:text-gray-100"
          >
        </div>

        <!--
          TOTP code goes last, immediately above the submit button. Authenticator codes roll
          every 30s, so anything typed after the code eats into its lifetime; entering it last
          means it is submitted within seconds of being read off the phone.
        -->
        <div>
          <label for="totpToken" class="block mb-1 text-sm font-medium text-gray-700 dark:text-gray-300">
            Authenticator code:
          </label>
          <input
            type="text"
            id="totpToken"
            name="totpToken"
            required
            <% if (locals.draft && draft.password) { %>autofocus<% } %>
            maxlength="6"
            inputmode="numeric"
            autocomplete="one-time-code"
            placeholder="123456"
            class="w-full px-4 py-2.5 border border-gray-200 dark:border-gray-700 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-500 bg-gray-50 dark:bg-gray-800 text-gray-900 dark:text-gray-100 tracking-widest text-lg text-center font-mono"
          >
          <p class="mt-1.5 text-xs text-gray-400 dark:text-gray-500">
            Enter this last — the code changes every 30 seconds.
          </p>
        </div>

        <div class="flex flex-col sm:flex-row justify-between items-center gap-4 pt-2">
          <button type="submit"
            class="w-full sm:w-auto inline-flex items-center justify-center gap-1.5 px-6 py-2.5 rounded-lg text-sm font-medium bg-green-600 text-white hover:bg-green-700 transition shadow-sm">
            <i class="bi bi-shield-lock"></i> Reset Password
          </button>
          <a href="/user/forgot-password"
            class="w-full sm:w-auto inline-flex items-center justify-center gap-1.5 text-center text-sm font-medium border border-gray-200 dark:border-gray-700 text-gray-700 dark:text-gray-300 px-6 py-2.5 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-800 transition">
            <i class="bi bi-arrow-left"></i> Start over
          </a>
        </div>
      </form>
    </div>
  </div>
</div>
