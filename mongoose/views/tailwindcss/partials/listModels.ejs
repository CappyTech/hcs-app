<div class="max-w-6xl mx-auto py-10 px-4">
  <div class="bg-gray-100 dark:bg-gray-800 rounded-xl p-6 shadow">
    <h1 class="text-3xl font-bold text-center mb-6">
      <%= title %>
    </h1>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mt-4">
      <% models.forEach(model => { %>
        <div class="flex flex-col bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-700 rounded-lg shadow p-4">
          <div class="flex-grow">
            <h5 class="text-xl font-semibold mb-2">
              <%= model.title %>
            </h5>
            <p class="text-sm text-gray-600 dark:text-gray-300">
              <%= model.description %>
            </p>
          </div>
          <a href="<%= model.link %>" class="mt-4 inline-block <%= model.buttonClass || 'bg-green-700 hover:bg-green-800' %> text-white text-sm px-5 py-2 rounded transition">
            <%= model.title %>
          </a>
        </div>
      <% }) %>
    </div>
  </div>
</div>
