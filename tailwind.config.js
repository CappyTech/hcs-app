/**** Tailwind config ****/
/** @type {import('tailwindcss').Config} */
// Auto-merge generated safelist (if present) with static entries.
let generatedSafelist = [];
try {
  // Generated by scripts/generate-tailwind-safelist.js
  generatedSafelist = require('./tailwind.safelist.js');
  if (!Array.isArray(generatedSafelist)) generatedSafelist = [];
} catch (_) {
  generatedSafelist = [];
}

const staticSafelist = [
  // Dashboard tile dynamic button classes (not directly in templates)
  'bg-blue-700', 'hover:bg-blue-800',
  'bg-green-700', 'hover:bg-green-800',
  'bg-blue-800', 'bg-green-800',
  // Common text colors potentially applied via config
  'text-green-600', 'text-green-700', 'text-blue-700'
];

module.exports = {
  darkMode: 'class',
  content: [
    './mongoose/views/**/*.ejs',
    './public/js/**/*.js',
    './mongoose/config/**/*.js'
  ],
  safelist: Array.from(new Set([...staticSafelist, ...generatedSafelist])),
  theme: {
    extend: {
      colors: {
        brand: {
          DEFAULT: '#047857',
          light: '#10b981',
          dark: '#065f46'
        }
      }
    }
  },
  plugins: [
    require('@tailwindcss/forms'),
    require('@tailwindcss/typography')
  ]
};
